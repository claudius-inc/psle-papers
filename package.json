{
  "name": "PrimarySchoolExamPapers",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "rm -rf .output && npm run seo:sitemap && nuxt generate",
    "preview": "nuxt preview",
    "seo:audit": "node scripts/audit-seo-output.mjs",
    "seo:audit:live": "node scripts/audit-live-seo.mjs",
    "seo:sitemap": "node scripts/generate-sitemap.mjs",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.2.2",
    "vue": "^3.5.26",
    "vue-router": "^4.6.4"
  }
}
